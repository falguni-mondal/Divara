import React from 'react'

const SectionHeading = ({text}) => {
  return (
    <h2 className='uppercase block w-fit mx-auto text-[6vw] tracking-wider'>
        {text}
    </h2>
  )
}

export default SectionHeading