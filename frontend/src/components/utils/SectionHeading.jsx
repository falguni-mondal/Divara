import React from 'react'

const SectionHeading = ({text}) => {
  return (
    <span className='uppercase block w-fit mx-auto text-[6vw] tracking-wider'>
        {text}
    </span>
  )
}

export default SectionHeading